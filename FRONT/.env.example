# Copie para .env.local e ajuste se a API não estiver em localhost:8080
# Use o MESMO host que você usa no navegador (localhost ou 127.0.0.1) para evitar bloqueio de CORS.
NEXT_PUBLIC_API_URL=http://localhost:8080
